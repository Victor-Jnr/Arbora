# Apps

Future desktop shell / UI lives here (Electron, Tauri, or native).

Stage 1 ships an interactive CLI under `src/arbora/cli/` so the plan → approve → execute loop is runnable without a GUI stack.
