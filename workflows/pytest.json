{
  "id": "pytest",
  "name": "Run pytest",
  "description": "Run python -m pytest in the current directory behind the broker.",
  "goal_phrases": [
    "pytest pack",
    "run pytest pack"
  ],
  "rationale": "Workflow pack: execute the local pytest suite without git writes or package installs.",
  "steps": [
    {
      "adapter": "terminal",
      "action": "run_powershell",
      "args": {
        "command": "python -m pytest --version 2>&1 | Out-String",
        "timeout_seconds": 30
      },
      "summary": "Check that python -m pytest is available",
      "sensitivity": "read",
      "side_effects": ["Runs a version query in PowerShell"]
    },
    {
      "adapter": "terminal",
      "action": "run_powershell",
      "args": {
        "command": "python -m pytest -q --tb=short 2>&1 | Out-String",
        "timeout_seconds": 180
      },
      "summary": "Run pytest in the current directory (may write .pytest_cache)",
      "sensitivity": "mutate",
      "side_effects": ["Executes the project test suite"]
    }
  ]
}
